= simple_form_for @page, url: update_page_path, method: :put, html: {class: 'form-vertical'} do |f|
  .form-inputs
    = f.input :content, as: :text, label: false, :input_html => { :style => 'width: 100%' }
  .form-actions
    = f.submit 'save', class: 'btn btn-primary'
